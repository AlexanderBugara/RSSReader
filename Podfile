platform :ios, '7.0'

target 'RSSReader' do
  use_frameworks!
  pod 'Sequencer'
  pod 'Masonry'
  target :RSSReaderTests do
	inherit! :search_paths
	pod 'Specta'
        pod 'Expecta', '~> 1.0'
        pod 'OCMockito', '~> 4.0'
	pod 'Nocilla'
  end
end
